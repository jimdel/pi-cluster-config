apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: jimdel
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: jimdel
  policy:
    alphabetical:
      order: asc  # This will select 'latest' if it exists